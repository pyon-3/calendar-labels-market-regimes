# Scripts

Public scripts in this directory should be sanitized before release.

The current public package intentionally includes derived figures and aggregated tables, but excludes raw API caches and full vendor-derived price panels.

Rules:

- do not commit `.env`
- do not commit API keys
- do not commit raw J-Quants responses
- do not commit full price caches
- keep reproducibility notes focused on methodology and public data policy
